/**
 * KONEX React Native Compressor Mock for Web
 * Production Ready
 */

export default {
  compressImage: async () => null,
  compressVideo: async () => null,
  getImageSize: async () => ({ width: 0, height: 0 }),
};

export const compressImage = async () => null;
export const compressVideo = async () => null;